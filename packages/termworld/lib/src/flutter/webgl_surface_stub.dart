import 'package:flutter/widgets.dart';
import 'package:termworld/src/core/terminal.dart';

/// Returns the active browser WebGL surface, when one owns [terminal].
Widget? terminalWebglSurface(Terminal terminal) => null;
